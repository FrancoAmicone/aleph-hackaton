# 🍐 The Great Pear

The game lives here. The full README — what it is, how to install it from the
`pear://` link, the rules, the flags and how the OTA pipeline is built — is at the
root of the repository:

**[`../README.md`](../README.md)**

Quick start from source:

```sh
npm install
npm start          # dev mode — updates disabled
npm test           # 138 tests
```
